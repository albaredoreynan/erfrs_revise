.row.flash-msg style="margin-top: 2%; position: absolute; width: 100%; margin-top: 10%;"
  .col-md-4.col-md-offset-4
    - flash.each do |key, value|
      - unless value =~ /Signed in successfully/
        div class = "#{flash_class(key)}"
          = value
